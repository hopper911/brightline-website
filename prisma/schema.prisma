// Prisma schema for portfolio media assets (SQLite for local dev)
generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = env("DATABASE_URL")
}

model MediaAsset {
  id           String    @id @default(cuid())
  // Use string here for SQLite; emulate enum in app layer
  kind         String
  title        String
  subtitle     String?
  description  String?
  url          String    @unique
  thumbnailUrl String?
  width        Int?
  height       Int?
  durationSec  Int?
  altText      String?
  aspectRatio  String?
  featured     Boolean   @default(false)
  sortOrder    Int       @default(0)
  source       String?
  createdAt    DateTime  @default(now())
  updatedAt    DateTime  @updatedAt

  tags         MediaAssetTag[]
}

model Tag {
  id        String          @id @default(cuid())
  name      String          @unique
  createdAt DateTime        @default(now())
  assets    MediaAssetTag[]
}

model MediaAssetTag {
  assetId String
  tagId   String

  asset MediaAsset @relation(fields: [assetId], references: [id], onDelete: Cascade)
  tag   Tag        @relation(fields: [tagId], references: [id], onDelete: Cascade)

  @@id([assetId, tagId])
}
